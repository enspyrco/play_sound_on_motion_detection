name: play_sound_on_motion_detection
description: Play a sound on motion detection with Raspberry Pi & Dart.
version: 0.0.0-a1
repository: https://github.com/enspyrco/play_sound_on_motion_detection

environment:
  sdk: ^3.0.0

dependencies:
  rpi_gpio: ^0.9.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
